config:
  clients:
    - url: todo
      basic_auth:
        username: ${GRAFANA_CLOUD_USERNAME}
        password: ${GRAFANA_CLOUD_PASSWORD}

extraArgs:
  - -config.expand-env=true

extraEnvFrom:
  - secretRef:
      name: grafana-cloud
