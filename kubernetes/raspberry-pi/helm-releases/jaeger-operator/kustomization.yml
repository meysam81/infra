configurations:
  - kustomizeconfig.yml

configMapGenerator:
  - files:
      - values.yaml=./helm-values.yml
    name: jaeger-operator-config

resources:
  - repository.yml
  - release.yml
  # - externalsecret.yml
  # - httproute.yml
