---
- name: Install flux-system bootstrap manifests
  kubernetes.core.k8s:
    definition: "{{ lookup('kubernetes.core.kustomize', dir='https://github.com/meysam81/infra//kubernetes/hetzner-personal/flux-system?timeout=20&ref=main') }}"
    state: present
    wait: true
    wait_timeout: 30
