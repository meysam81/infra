configurations:
  - kustomizeconfig.yml

configMapGenerator:
  - files:
      - values.yaml=helm-values.yml
    name: hcloud-csi-driver-values

resources:
  - helmrelease.yml
  - externalsecret.yml

namespace: flux-system
