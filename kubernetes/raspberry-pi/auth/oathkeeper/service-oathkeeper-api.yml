apiVersion: v1
kind: Service
metadata:
  name: oathkeeper-api
  labels:
    app.kubernetes.io/instance: oathkeeper
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http-api
  selector:
    app.kubernetes.io/instance: oathkeeper
    app.kubernetes.io/name: oathkeeper
  type: ClusterIP
