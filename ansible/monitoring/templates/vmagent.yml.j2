global:
  scrape_interval: 10s

scrape_configs:
  # - job_name: haproxy
  #   file_sd_configs:
  #     - files:
  #         - /etc/prometheus/targets/haproxy/*.yml

  - job_name: node-exporter
    static_configs:
      - targets:
          - localhost:9100
        labels:
          instance: hetzner-personal
