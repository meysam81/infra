configMapGenerator:
  - name: mongodb-values
    files:
      - values.yaml=./values.yml

resources:
  - namespace.yml
  - repository.yml
  - root-password.yml
  - password.yml
  - secrets.yml
  # - release.yml

configurations:
  - kustomizeconfig.yml

namespace: mongodb
