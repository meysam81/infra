configMapGenerator:
  - name: mongodb-config
    envs:
      - configs.env

resources:
  - namespace.yml
  - password.yml
  - secrets.yml
  - push-secret.yml
  - statefulset.yml

images:
  - name: mongo
    newTag: "7"

namespace: mongodb
