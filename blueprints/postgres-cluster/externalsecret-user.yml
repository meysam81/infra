apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-listmonk
spec:
  data:
    - remoteRef:
        key: /postgres/listmonk/username
      secretKey: username
    - remoteRef:
        key: /postgres/listmonk/password
      secretKey: password
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-parameter-store
  target:
    template:
      type: kubernetes.io/basic-auth
