configurations:
  - kustomizeconfig.yml

configMapGenerator:
  - files:
      - values.yaml=./external-secrets/values.yml
    name: external-secrets-config
    namespace: flux-system

resources:
  - ./cert-manager/kustomize.yml
  - external-secrets/helm-release.yml
  - prometheus/helm-release.yml
  - kratos/helm-release.yml
