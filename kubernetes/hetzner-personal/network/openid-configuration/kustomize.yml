apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: openid-configuration
  namespace: flux-system
spec:
  force: false
  interval: 1m
  path: ./kubernetes/hetzner-personal/network/openid-configuration
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
