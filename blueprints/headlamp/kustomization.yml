configurations:
  - kustomizeconfig.yml

configMapGenerator:
  - files:
      - values.yaml=helm-values.yml
    name: headlamp

resources:
  - helmrepository.yml
  - helmrelease.yml

namespace: flux-system
