quiet: false
debug: false
insecure: false
timeout: 1m
cache:
  dir: .pre-commit-trivy-cache
scan:
  skip-dirs:
    - "**/.terraform*"
misconfiguration:
  terraform:
    exclude-downloaded-modules: false
severity:
  - HIGH
  - CRITICAL
