# ... truncated for brevity ...
# NOTE: Again, in a production scenario, this would contain the full contents
# of the source helm values file.

auth:
  usernames:
    - app
  databases:
    - app

persistence:
  enabled: false
