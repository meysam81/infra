configurations:
  - kustomizeconfig.yml

configMapGenerator:
  - files:
      - values.yaml=helm-values.yml
    name: atlantis

resources:
  - externalsecret.yml
  - helmrelease.yml
  - httproute.yml

namespace: flux-system
