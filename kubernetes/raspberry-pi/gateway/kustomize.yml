apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gateway
  namespace: flux-system
spec:
  force: false
  interval: 10m0s
  path: ./kubernetes/raspberry-pi/gateway
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
