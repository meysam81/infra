---
downloads_dir: /tmp
promtail_source_binary_name: "{{ promtail_url | basename | regex_replace('\\.zip$', '') }}"
fail2ban_exporter_enabled: false

loki_remote_write_password_file: /etc/promtail/loki-password
