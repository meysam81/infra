apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: submit-hackernews
spec:
  data:
    - remoteRef:
        key: /hackernews/password
      secretKey: HACKERNEWS_PASSWORD
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-parameter-store
